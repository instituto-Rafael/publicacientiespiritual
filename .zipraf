# 1. salvar o manifesto
echo '<conteúdo acima>' > manifest_root.json

# 2. comprimir em zipraf
zip -j RAFAELIA_CORE_ROOT_20250831.zipraf manifest_root.json

# 3. (opcional) assinar com Ed25519
openssl pkeyutl -sign -inkey keys/chave_privada.pem \
  -in manifest_root.json -out manifest_root.sig
base64 -w0 manifest_root.sig > manifest_root.sig.b64
